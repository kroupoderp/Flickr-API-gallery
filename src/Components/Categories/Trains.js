

import Gallery from '../Gallery'


class Trains extends Gallery {}
export default Trains;

// Looking at App.js, the Switch component won't work as intended if the exact same
// component is being rendered for different routes.